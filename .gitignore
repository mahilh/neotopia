# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment — never commit secrets
.env.local
.env.*.local
.env.production

# Claude Code MCP config — contains Supabase PAT (secret)
# Keep .mcp.json LOCAL ONLY — never push to GitHub
.mcp.json

# Session comms between terminals (local only)
.claude/comms/

# Playwright MCP browser-verification output (local only)
.playwright-mcp/

# Playwright E2E artifacts (generated · never commit)
/test-results/
/playwright-report/
/playwright/.cache/
.claude/service-account-key.json
